# Hooks

Custom React hooks. Patterns:

- `useX` naming.
- Keep side effects minimal.
- Export directly from file; optional barrel file if needed.

Examples include `use-media-query`, `use-mobile`, `use-breadcrumbs`.
