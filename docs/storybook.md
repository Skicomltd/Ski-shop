# Storybook & Chromatic

## Storybook

Run locally:

```bash
pnpm storybook
```

Build static site:

```bash
pnpm build-storybook
```

## Chromatic

Chromatic is configured (see `chromatic.config.json` and workflow under `note/workflows/chromatic.yml`).

Publish from CI for visual regression testing and review.
