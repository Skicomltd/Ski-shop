# Mocks

Mock Service Worker (MSW) is configured under `src/mocks/`. See `src/mocks/README.md` for full details.

Quick toggle via env vars in `.env.local`:

```
NEXT_PUBLIC_ENABLE_MOCK=true
NEXT_PUBLIC_LOG_MOCK_REQUESTS=true
```
